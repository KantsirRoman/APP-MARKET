<UserControl x:Class="MyApp.UserComponent.UserDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyApp.UserComponent"
             Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Message1}" Margin="10" FontSize="16" FontWeight="Bold" />

        <TextBlock Text="{Binding Message2}" Margin="10" Grid.Row="1" FontSize="16" FontWeight="Bold" />

        <TextBlock Text="{Binding Message3}" Margin="10" Grid.Row="2" FontSize="16" FontWeight="Bold" />

        <Button Content="OK" Margin="10" Grid.Row="3" HorizontalAlignment="Center" Width="100" Height="30" Click="Button_Click"/>
    </Grid>
</UserControl>
